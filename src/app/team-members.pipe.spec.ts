import { TeamMembersPipe } from './team-members.pipe';

describe('TeamMembersPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamMembersPipe();
    expect(pipe).toBeTruthy();
  });
});
